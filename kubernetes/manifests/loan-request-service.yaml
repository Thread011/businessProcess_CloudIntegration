apiVersion: apps/v1
kind: Deployment
metadata:
  name: loan-request-service
  namespace: loan-system
  labels:
    app: loan-request-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: loan-request-service
  template:
    metadata:
      labels:
        app: loan-request-service
    spec:
      containers:
      - name: loan-request-service
        image: localhost/loan-request-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: loan-system-config
        - secretRef:
            name: loan-system-secrets
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "300m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: loan-request-service
  namespace: loan-system
spec:
  selector:
    app: loan-request-service
  ports:
  - port: 8000
    targetPort: 8000
  type: ClusterIP
