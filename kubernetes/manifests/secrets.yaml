apiVersion: v1
kind: Secret
metadata:
  name: loan-system-secrets
  namespace: loan-system
type: Opaque
stringData:
  RABBITMQ_PASSWORD: "guest"
  MARKET_DATA_API_KEY: "your-api-key-here"
  SMTP_HOST: "smtp.example.com"
  SMTP_PORT: "587"
  SMTP_USERNAME: "notification@example.com"
  SMTP_PASSWORD: "password"
  EMAIL_SENDER: "loan-system@example.com"
